[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
nicegui = ">=1.4"
pymongo = ">=4.7"
pydantic = ">=2.0"
httpx = ">=0.27"
python-dotenv = ">=1.0"
py-mini-racer = ">=0.6"

[dev-packages]

[requires]
python_version = "3.11"

[scripts]
start = "python app.py"
dev = "python -c \"import os; os.environ.setdefault('NICEGUI_RELOAD', '1'); exec(open('app.py').read())\""
install = "pipenv install"
